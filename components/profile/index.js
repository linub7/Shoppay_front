import ProfilePageComponentLayout from './layout';

const ProfilePageComponent = ({ me, tab }) => {
  return (
    <ProfilePageComponentLayout me={me} tab={tab}>
      index
    </ProfilePageComponentLayout>
  );
};

export default ProfilePageComponent;
